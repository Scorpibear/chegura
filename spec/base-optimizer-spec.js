describe('base-optimizer', function () {
    var baseOptimizer = require('../app/base-optimizer')
    describe('optimize', function () {
        /*it('calls asynchronious', function (done) {
            //baseOptimizer.optimize({ m: '' });
            //TODO: Need solution how to test that baseIterator.getMovesToInsufficientEvaluationDepth is called 
            //      only in 1 second after baseOptimizer.optimize call
            done()
        })*/
    })
})